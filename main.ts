/**
 * Command Group Plugin for Obsidian
 *
 * Entry point for the plugin.
 * The main implementation is in src/plugin.ts
 */

import { CommandGroupPlugin } from './src/plugin';

export default CommandGroupPlugin;
